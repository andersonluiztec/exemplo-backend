steps:
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
      [
        'compute',
        'instances',
        'reset',
        'vm-exemplo-backend',
        '--zone=us-central1-f'
      ]

options:
  logging: CLOUD_LOGGING_ONLY